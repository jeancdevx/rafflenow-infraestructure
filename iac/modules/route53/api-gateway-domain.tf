# API Gateway custom domains removed
# APIs are now accessed only through CloudFront at:
# - https://rafflenow.es/api/v1/public/raffles (Public API)
# - https://rafflenow.es/api/v1/raffles (Authenticated API)

# The API certificate and custom domains are no longer needed
# since all API traffic goes through CloudFront which has its own certificate
